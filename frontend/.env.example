# Backend API (teammate's Express server). Not used until later milestones.
VITE_API_URL=http://localhost:5000

# Supabase Auth (used on the frontend for login/signup).
# Leave blank locally and the Login page will show a friendly notice
# instead of crashing.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
